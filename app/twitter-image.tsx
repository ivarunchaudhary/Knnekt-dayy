/**
 * X reads `twitter:image` before falling back to `og:image`, so the summary card
 * is pointed at the same generated preview rather than left to the fallback.
 */
export { default, alt, size, contentType } from "./opengraph-image";
